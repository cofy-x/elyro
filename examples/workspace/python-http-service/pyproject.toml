[project]
name = "python-http-service"
version = "0.1.0"
description = "Generic Python HTTP service example for Elyro Workspace"
readme = "README.md"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
